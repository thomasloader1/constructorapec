mixin galeriaDeObras(obras)
  each obra, i in obras
    article(class=`item-obra-${i}`)
      .d-flex.align-items-center.justify-content-center
        p= obra.title
    +modalParaLasObras(obra)

mixin modalParaLasObras(objObra)
  //  Button trigger modal 
  button.btn.btn-primary(type="button", data-toggle="modal", data-target="#centralModalSm") Launch demo modal
  //  Central Modal Small 
  .modal.fade#centralModalSm(tabindex="-1", role="dialog", aria-labelledby="myModalLabel", aria-hidden="true")
    //  Change class .modal-sm to change the size of the modal 
    .modal-dialog.modal-sm(role="document")
      .modal-content
        .modal-header
          h4.modal-title.w-100#myModalLabel Modal title
          button.close(type="button", data-dismiss="modal", aria-label="Close")
            span(aria-hidden="true") ×
        .modal-body.
          
          ...
          
        .modal-footer
          button.btn.btn-secondary.btn-sm(type="button", data-dismiss="modal") Close
          button.btn.btn-primary.btn-sm(type="button") Save changes
  //  Central Modal Small 